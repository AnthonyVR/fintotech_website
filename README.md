# Fintotech Website

Simple static site for GitHub + Vercel deployment.

## Deploy

Push this folder to a GitHub repository and import the repo into Vercel. No build
configuration is required because the site is just `index.html`.
